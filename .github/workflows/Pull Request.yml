name: CI on Pull Request

on:
  pull_request:
    branches:
      - '**'

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2.1.0

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Setup msbuild
      uses: microsoft/setup-msbuild@v1

    - name: Setup VSTest
      uses: darenm/Setup-VSTest@v1

    - name: Restore NuGet packages
      run: |
        nuget restore "Hunter Industries API.sln"
        dotnet restore "Hunter Industries API Control Panel/Hunter Industries API Control Panel.csproj"

    - name: Navigate to workspace
      run: cd $GITHUB_WORKSPACE

    - name: Build .NET Framework projects
      run: |
        MSBuild.exe "Hunter Industries API/Hunter Industries API.csproj" /p:configuration="Release"
        MSBuild.exe "Hunter Industries API.Tests/Hunter Industries API.Tests.csproj" /p:configuration="Release"

    - name: Build .NET projects
      run: |
        dotnet build "Hunter Industries API Common/Hunter Industries API Common.csproj" --configuration Release
        dotnet build "Hunter Industries API Control Panel/Hunter Industries API Control Panel.csproj" --configuration Release

    - name: Run tests
      run: vstest.console.exe ".\Hunter Industries API.Tests\bin\Release\Hunter Industries API.Tests.dll"
